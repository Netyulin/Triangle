export const metadata = {
  title: "Triangle - Software Detail",
  description: "Triangle software detail page",
}

export default function SoftwareDetailLayout({ children }: { children: React.ReactNode }) {
  return children
}
