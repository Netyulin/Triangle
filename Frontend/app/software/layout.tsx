export const metadata = {
  title: "Triangle - Software",
  description: "Triangle software listing page",
}

export default function SoftwareLayout({ children }: { children: React.ReactNode }) {
  return children
}
