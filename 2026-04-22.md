# 2026-04-22 变更记录

## 软件后台编辑页
- 新建/编辑软件的“摘要说明”编辑器改为与文章编辑页一致的双模式：`所见即所得预览` + `HTML 源码`。
- 在 `HTML 源码` 模式下，支持上传图片并自动插入 `<img>`，也支持粘贴图片直接上传插入。
- 保留并完善“软件截图”管理：可上传、手动添加地址、预览、删除。

## 前端 HTML 样式
- 为前端启用 Tailwind Typography 插件，`summary` 的 HTML 内容在 `prose` 容器中可获得默认排版样式。

## 环境与依赖变更
- Frontend 新增开发依赖：`@tailwindcss/typography`
- 配置修改：`Frontend/tailwind.config.js` 增加 `plugins: [require("@tailwindcss/typography")]`

## 当日补充修复（2026-04-22 晚）
- 修复 `POST /api/analytics/page-view` 在 `referrer=null` 时触发 400 的问题：
  - 后端校验放宽为 `nullable`；
  - 前端上报改为无 referrer 时不传该字段。
- 修复软件页 SEO 标题英文占位文案，改为中文站点标题：
  - `Frontend/app/software/layout.tsx`
  - `Frontend/app/software/[slug]/layout.tsx`
- 下调站点 Logo 图片 preload 优先级（移除默认 `priority`），减少浏览器 “preloaded but not used” 警告触发概率。

## 当日补充修复（2026-04-22 夜）
- 下载权限策略调整：所有软件下载（包括 `free`）均要求登录后才能获取下载链接。
  - 修改文件：`backend/src/controllers/appController.js`
  - 行为变化：未登录用户访问 `GET /api/apps/:slug/access` 时，`downloadPermission` 返回 `login required`。
- 下载体验分级：赞助会员及以上（`sponsor/lifetime/supreme` 及兼容别名）下载时无需倒计时，且自动隐藏下载页广告位。
  - 修改文件：`Frontend/app/software/[slug]/page.tsx`
  - 修改文件：`Frontend/app/download/[slug]/page.tsx`

## 当日补充修复（2026-04-22 深夜）
- 软件详情页新增“资源使用声明”容器，统一展示下载使用合规声明，覆盖全部软件详情页。
  - 修改文件：`Frontend/app/software/[slug]/page.tsx`
- 修复上传清理脚本误判站点路由为“缺失图片引用”的问题（例如 `/pricing` 不再被识别成上传文件路径）。
  - 修改文件：`backend/scripts/cleanup-unused-upload-images.mjs`

## 当日补充修复（2026-04-22 夜深）
- 文章详情页增加“正文兼容渲染”：
  - 当 `content` 里是完整 HTML 文档（含 `<html>/<head>/<body>/<style>`）时，自动提取 `<body>` 内容并移除 `style/script`，避免全站样式被污染。
  - 当 `content` 为空时，页面显示可读兜底文案（优先 `excerpt` / `seoDescription`），避免出现空白正文区域。
  - 修改文件：`Frontend/app/articles/[slug]/page.tsx`
